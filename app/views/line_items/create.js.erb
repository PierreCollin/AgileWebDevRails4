$('#notice').hide();

if ($('#cart tr').length == 1) { // only the total row
  $('#cart').show('blind', 1000);
}

$('#cart').html("<%= escape_javascript render(@cart) %>");

$('#current_item').css(
  {
    'background-color': '#8F8'
  }).animate(
  {
    'background-color': '#141'
  }, 1000
);
